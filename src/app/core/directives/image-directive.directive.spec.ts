import { ImageDirectiveDirective } from './image-directive.directive';

describe('ImageDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
